"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../utils");
function isLabelElement(node) {
    return node.type === "VElement" && utils_1.getElementType(node) === "label";
}
function hasLabelElement(node) {
    const { parent } = node;
    return ([parent, ...parent.children].some(isLabelElement) ||
        (parent && parent.type === "VElement" && hasLabelElement(parent)));
}
const rule = {
    meta: {
        docs: {
            url: utils_1.makeDocsURL("form-has-label")
        },
        messages: {
            default: "Each form element must have a programmatically associated label element."
        }
    },
    create(context) {
        return utils_1.defineTemplateBodyVisitor(context, {
            VElement(node) {
                const elementType = utils_1.getElementType(node);
                if (!["input", "textarea"].includes(elementType)) {
                    return;
                }
                if (elementType === "input") {
                    const type = utils_1.getElementAttributeValue(node, "type");
                    if (!type ||
                        ["hidden", "button", "image", "submit", "reset"].includes(type)) {
                        return;
                    }
                }
                if (!utils_1.isAriaHidden(node) &&
                    !utils_1.hasAriaLabel(node) &&
                    !hasLabelElement(node)) {
                    context.report({ node: node, messageId: "default" });
                }
            }
        });
    }
};
exports.default = rule;
